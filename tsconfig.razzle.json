{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    /**
     * Output ES modules for razzle, which is going to run everything through webpack.
     * In the main tsconfig.json, we want this to be 'commonjs' so that node.js
     * can interpret the output.
     */
    "module": "esnext"
  }
}
